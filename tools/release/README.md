# Release Tools

This directory contains maintainer-only release and repository-preparation utilities.

Typical users of the skill should not need anything here.

Primary user-facing entrypoints remain under:

- `scripts/install.sh`
- `scripts/doctor.sh`
- `scripts/mode.sh`
- `scripts/advanced-preflight.sh`
- `scripts/prepare-browser.sh`
